on:
  push:
    branches:
      - develop

name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 📦 Install dependencies
      run: npm install

    - name: 🏗️ Build
      run: npm run build

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: sftp://82.112.247.245
        port: 65002
        username: u903934307
        password: K/99uQoUR$#QT[Hh
        ignore: |
          **/.git*
          **/.git*/**
          **/.vscode/**
          **/node_modules/**
          **/custom-plugins/**
        uploads: |
          ./ => wp-content/themes/darkjail/